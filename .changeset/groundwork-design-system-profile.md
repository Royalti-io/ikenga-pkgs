---
"@ikenga/skill-groundwork": minor
---

Ship the `design-system` profile — adds `parts-template.md` + `quality-gate.md` to the spine (work unit: "part"; isolation: tier + domain; per-part Nielsen/WCAG quality gate). Sync the skill tree to current dev (board template, living-spec, `lib/schemas.md`, `orchestrate.workflow.js` template, `groundwork_state.py`, action/agent docs). README now names Gemini and all four profiles (was "software and general").
