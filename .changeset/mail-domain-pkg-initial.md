---
"@ikenga/pkg-mail": minor
---

Add `com.ikenga.mail` domain pkg (WP-17b) — Inbox / Triage / All / Drafts views over the local `ikenga.db` mail schema, deterministic CSS vendoring, thread-state read/write (mark-read, snooze 4h, tag), and AppBridge `host.dbQuery` / `host.dbExec` data path.
