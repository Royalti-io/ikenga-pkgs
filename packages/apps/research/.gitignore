node_modules/
# This is a thin no-build pkg — dist/ is the source, not a build output.
# Override the workspace-level `dist/` ignore.
!dist/
!dist/**
# But exclude the vendored CSS strings (regenerated by scripts/build.mjs from @ikenga/tokens).
dist/lib/tokens-css.js
dist/lib/app-kit-css.js
