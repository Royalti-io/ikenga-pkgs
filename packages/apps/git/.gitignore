node_modules/
*.tsbuildinfo

# sidecar/dist, mcp/dist AND ui/dist are all build outputs now (WP-06 built
# the UI as a vite bundle, same convention as com.ikenga.studio's dist/, not
# the hand-authored com.ikenga.tasks dist/ pattern the WP-02 scaffold
# originally followed) — none are tracked; `npm run build` regenerates them.
