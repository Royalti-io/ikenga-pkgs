{
  "name": "@ikenga/pkg-git-ui",
  "version": "0.0.0",
  "private": true,
  "description": "com.ikenga.git iframe UI (WP-06) — side-menu shell (Changes · History · Branches · Worktrees · PRs), theme mirroring, no-root empty states, mocked-sidecar dev mode. Bundled by vite into the PKG ROOT's ../dist (not ui/dist) — the shell hardcodes an iframe pkg's content root to <pkg>/dist, so that is where manifest.json's ui.routes source `dist/index.html` resolves.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -p tsconfig.json --noEmit && vite build",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node --test --import=tsx --import=./src/test/register.mjs 'src/**/*.test.ts'",
    "preview": "vite preview"
  },
  "dependencies": {
    "@modelcontextprotocol/ext-apps": "^1.7.1",
    "@modelcontextprotocol/sdk": "^1.23.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0",
    "vite": "^5.4.0"
  }
}
