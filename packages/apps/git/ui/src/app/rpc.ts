// com.ikenga.git · UI-side re-export of the frozen G-RPC contract.
//
// Was a verbatim copy of plans/git/drafts/rpc.ts until WP-03 landed
// core/src/rpc.ts (byte-identical to the frozen draft) — this file's own
// header used to say as much: "become a re-export ... once WP-03 lands".
// WP-03 landed; this is that re-export.
export * from '../../../core/src/rpc';
