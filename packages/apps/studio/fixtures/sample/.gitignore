# Generated by the studio render queue / exporter — never committed.
# The committed fixture is source only (storyboard.json + cells/).
renders/
exports/

# SQLite render-queue DB if the sidecar is pointed here via IKENGA_PKG_DATA.
studio.db
studio.db-*
